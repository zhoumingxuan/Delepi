# Delepi 预置 Python 环境依赖清单（内置 Python 3.14.6 预置环境）
# 与 scripts/build-preset-python.ts 的 PRESET_PACKAGES 常量保持一致（31 包，顺序一致）
# 安装方式：pip install -r requirements-preset.txt
# 说明：TCP/UDP 通信由 Python 标准库 socket 支持，无需第三方包
#
# ---- yaml文件读写 ----
PyYAML>=6.0.2
# ---- PDF 读写 ----
pdfplumber>=0.11.0
PyPDF2>=3.0.0
# ---- Word 读写 ----
python-docx>=1.1.0
# ---- Excel/CSV 读写（openpyxl 同时覆盖 Excel 和 CSV 读写）----
openpyxl>=3.1.0
# ---- 折线图（画图/曲线图渲染）----
matplotlib>=3.9.0
# ---- HTTP 客户端 ----
requests>=2.32.0
httpx>=0.27.0
# ---- 图像处理（matplotlib 依赖，且 PDF 处理需要）----
Pillow>=10.4.0
# ---- 数据处理基础库（被多个上层包依赖）----
numpy>=2.1.0
# ---- PPT 读写 ----
python-pptx>=0.6.23
# ---- SSH ----
paramiko>=2.11
# ---- 组织架构图（配合 matplotlib 渲染，不使用需系统二进制的 graphviz）----
networkx>=3.0
# ---- 报表读写 ----
pandas>=1.5
# ---- 报表导出（Excel 写出）----
xlsxwriter>=3.0
# ---- HTML/XML/浏览器（2026-08-20 新增）----
beautifulsoup4>=4.12.0
html5lib>=1.1
xmltodict>=0.13.0
defusedxml>=0.7.1
playwright>=1.40.0
pygetwindow>=0.0.9
# ---- 数据库驱动与常用工具（2026-08-20 新增）----
pymysql>=1.2.0
mysql-connector-python>=26.7.0
pyodbc>=5.3.0
pymssql>=2.3.13
psycopg2-binary>=2.9.12
oracledb>=4.0.2
redis>=8.1.0
pymongo>=4.17.0
SQLAlchemy>=2.0.52
pydantic>=2.13.4
orjson>=3.12.0

# ---- Win10 专用增量（2026-08-22 新增：模拟键盘鼠标操控 / Win32 API / UIA 自动化）----
# pyautogui：键盘鼠标模拟（传递依赖 pymsgbox/pytweening/pyscreeze/mouseinfo/pyrect/pyperclip 由 pip 自动解决）
# pywinauto：Windows UI 自动化（UIA/Win32，传递依赖 comtypes）
# pynput：全局键盘鼠标监听与控制
# pywin32：Win32 API / COM 层（即“win10 SDK”；wheel 自带 pywin32.pth，嵌入式 _pth 下 import win32api 可用，2026-08-22 预演实测）
pyautogui>=0.9.54
pywinauto>=0.6.8
pynput>=1.7.7
pywin32>=306
