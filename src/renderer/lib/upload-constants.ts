/** 上传相关常量（渲染进程） */

export { MAX_UPLOAD_COUNT } from '@shared/constants';

/**
 * 判断 MIME 类型是否为图片
 * 统一从 shared/utils/image-type 导出
 */
export { isImageContentType } from '@shared/utils/image-type';
